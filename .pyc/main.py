import helper

print("Main program")
# Run in terminal by typing python3 main.py

# Python first converts .py files into bytecode. The compiled bytecode is stored as .pyc files inside the __pycache__ directory. This helps Python load modules faster in future executions because it can reuse the bytecode instead of recompiling the source code every time.
